name: Debug Shellon:  workflow_dispatch:jobs:  debug:    runs-on: ubuntu-latest  |          sudo apt update -y          sudo apt install -y tsudo apt update -y          sudo apt install -y tm: |          echo "[*] Starte session e      run: |          echo "[*] Starting tmate session..."          # إنشاء session بصمت في الخلفية          tm  e -S /tmp/tmate.sock new-session -d                    # انتظر حتى تصبح الجلسة جاهزة          tmate -S /tmp/tmate.sock wait tmate-ready          # عرض رابط SSH و Web URL          echo "[*] SSH command:"          tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}'          echo "[*] Web terminal URL:"          tmate -S /tmp/tmate.sock display -p '#{tmate_web}'       l  # اجعل الجلسة مستمرة          tail -f /dev/null
